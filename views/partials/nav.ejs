<% if (currentUser) { %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">Customer PO Tracking</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="/po-tracking">PO Tracking</a></li>
                    <% if (currentUser.role==='data_manager' || currentUser.role==='system_admin' ) { %>
                        <li class="nav-item"><a class="nav-link" href="/quotations">Quotation</a></li>
                        <li class="nav-item"><a class="nav-link" href="/customers">Customer</a></li>
                        <% } %>
                            <% if (currentUser.role==='system_admin' ) { %>
                                <li class="nav-item"><a class="nav-link" href="/users">User</a></li>
                                <% } %>
                </ul>
                <div class="text-white me-3">
                    <%= currentUser.name %> (<%= currentUser.role %>)
                </div>
                <form action="/logout" method="post">
                    <button class="btn btn-outline-light btn-sm" type="submit">Logout</button>
                </form>
            </div>
        </div>
    </nav>
    <% } %>